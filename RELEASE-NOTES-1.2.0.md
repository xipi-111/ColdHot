# ColdHot 1.2.0

## 新功能

- 可以选择一张本地图片作为整个菜单面板的背景，覆盖顶部状态、指标区、快捷控制和底部工具栏。
- 提供 0%–70% 的深色遮罩调节，默认 35%，指标卡片使用半透明材质保证可读性。
- 设置页支持背景预览、临时关闭、重新开启、更换和移除。

## 隐私与性能

- 背景图片只在本机处理，不会上传或联网。
- 导入时移除原始元数据并把最长边限制到 1600 px，以 PNG 副本保存在 Application Support 中。
- 应用只在启动或更换图片时读取背景；实时指标刷新不会重复读取、解码或缩放图片。
- 图片缺失、损坏或导入失败时自动使用系统材质背景，失败的导入不会覆盖现有有效图片。

## 发行说明

- 版本号：`1.2.0 (8)`。
- 本版本为官网 / GitHub 分发版。
- 系统要求为 macOS 14 或更高版本。
