# ColdHot 1.2.1

## 面板显示

- 自定义背景新增 10%–100% 的卡片不透明度调节，让背景图片可以按偏好充分透出。
- 新增 50%–100% 的文字亮度调节，并在使用自定义背景时加入轻微文字阴影。
- 文字调节覆盖顶部状态、指标卡片、展开详情、Dock 控制和底部工具栏，不会降低彩色图标、进度条和开关的亮度。

## 监控修复

- 修复 Apple Silicon 非 1:1 Mach timebase 下高 CPU 进程使用率被严重低估的问题。
- 内存“已用”改为与 macOS 活动监视器一致的 App/匿名、联动和压缩内存合计口径。
- 可回收的文件缓存和 purgeable 内存改为单独展示，不再计入主内存进度条。
- 修复重复读取 HID 温度事件时未释放 Copy 返回对象的问题。

## 发行说明

- 版本号：`1.2.1 (9)`。
- 本版本为官网 / GitHub 分发版。
- 系统要求为 macOS 14 或更高版本。
